VITE_API_BASE_URL=http://localhost:8000
# Pinata gateway for credential IPFS links (public gateways often return "Gone").
VITE_IPFS_GATEWAY_URL=https://gateway.pinata.cloud/ipfs
# Besu Chainlens explorer base URL (use /contracts/, not /nfts/ on free tier)
VITE_CHAINLENS_URL=http://localhost:8082
